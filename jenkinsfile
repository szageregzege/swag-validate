pipeline {
    agent any
    tools {
    nodejs '20.0.0'
    }
    
    
    stages {
        stage('Install Dependencies') {
            steps {
                sh 'npm version'
            }
        }
      
    }
}
