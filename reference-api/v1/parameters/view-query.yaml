description: |
  Describe the parameter here, `summary` by default.
in: query
required: false
name: view
schema:
  
  default: summary
  type: string
  
  example: detailed
  enum:
    - summary
    - detailed
