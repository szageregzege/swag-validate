# Demonstrates a mechanism to sort results by specific order or field names.
# See Sortable results in https://pulsar.akamai.com/services/apis/api-design-checklist/#configurable-collections
description: |
  Specifies the sort order of the returned collection.
in: query
required: false
name: sortBy
schema:
  type: string
  # Specify a typical 'example' value
  example: createdTime
